<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bagpunchergame">

    <supports-screens
        android:smallScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:normalScreens="true"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/boxing_glove"
        android:label="@string/bag_puncher"
        android:roundIcon="@drawable/boxing_glove"
        android:supportsRtl="true"
        android:theme="@style/Theme.BagPuncherGame">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".comboWindow"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop">
        </activity>

        <activity android:name=".speedbagWindow"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop">
        </activity>
    </application>

</manifest>