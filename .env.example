# Chat agent feature flag — set to 'true' when RAG backend is deployed
VITE_CHAT_ENABLED=false

# URL of the RAG backend (existing FastAPI app)
VITE_CHAT_API_URL=http://localhost:8000

# Formspree form ID for contact form (get one free at formspree.io)
# Replace YOUR_FORM_ID in src/pages/Contact.jsx with your actual ID
